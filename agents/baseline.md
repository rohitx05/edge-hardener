# Baseline agent instruction (one-shot)

You are given a UI component file. Rewrite it to be robust to edge-case data.
Return only the full updated file.

- Do NOT run any tools. One shot, no feedback loop.
- Same underlying model as the advanced agent.

> This is the honest floor. Do not cripple it — a fair baseline is what makes the
> Measured Improvement points credible. Its only disadvantage vs. advanced is the
> absence of the harness + verify loop, and the README discloses exactly that.
